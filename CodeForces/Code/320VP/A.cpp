#include <cstdio>
#include <cstdlib>
#include <algorithm>
#include <cstring>

using namespace std;

int get()

int main()
{
	int a,b,d;
	scanf("%d%d",&a,&b);
	double ans=2147483647,now,x;
	x=a-b;
	if (abs(x)<1e-6) now=b;
	else if (x<0) now=-1;
	else
	{
		d=int(x/b);
		
	}
	
} 
