#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    scanf("%d%d",&n,&m);
    vector<vector<int> >val(n, vector<int>(m, 0));
    for (int i=0;i<n;i++) {
        for (int j=0;j<m;j++) {
            scanf("%d",&val[i][j]);
        }
    }
    if (m <= 500) {
        for (int i=0;i<n;i++) {
            for ()
        }
    }
}